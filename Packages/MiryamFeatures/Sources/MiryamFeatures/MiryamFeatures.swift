// MiryamFeatures — @Observable @MainActor ViewModels, use cases, DI container, Router
// Depends on protocols only (via MiryamCore). Concrete actors injected at app startup.
public enum MiryamFeatures {}
